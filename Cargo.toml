[package]
name = "crustlet"
version = "0.1.0"
edition = "2024"

[dependencies]
log = "0.4.28"
uefi = { version = "0.35.0", features = ["alloc", "global_allocator", "logger", "qemu"] }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[[bin]]
name = "crustlet"
path = "src/main.rs"
